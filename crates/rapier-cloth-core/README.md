# rapier-cloth-core

Engine-independent CPU XPBD cloth solver. Use `rapier-cloth` for Rapier integration.

Select exactly one precision: `f32` (default), or `default-features = false, features = ["f64"]`.
